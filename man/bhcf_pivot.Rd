% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bhcf_pivot.R
\name{bhcf_pivot}
\alias{bhcf_pivot}
\title{Pivot long BHCF data to wide format using DuckDB}
\usage{
bhcf_pivot(
  data,
  id_cols = c("RSSD9001", "date"),
  names_from = "item",
  values_from = "value",
  items = NULL,
  values_fn = "first"
)
}
\arguments{
\item{data}{A DuckDB-backed lazy `tbl` containing long BHCF data.}

\item{id_cols}{Character vector of identifier columns in the wide output.
Defaults to `c("RSSD9001", "date")`.}

\item{names_from}{Character scalar with the column that contains variable
names (default `"item"`).}

\item{values_from}{Character scalar with the column that contains values
(default `"value"`).}

\item{items}{Optional character vector of item names to include.}

\item{values_fn}{Character scalar naming the DuckDB aggregate used when
multiple rows exist per `id_cols + names_from` combination. Default is
`"first"`.}
}
\value{
A DuckDB-backed lazy `tbl` in wide format.
}
\description{
Pivots a DuckDB-backed long BHCF table (typically with columns like
`RSSD9001`, `date`, `item`, `value`) into wide format.
}
